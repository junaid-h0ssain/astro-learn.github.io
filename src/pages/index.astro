---
import BaseLayout from '../layouts/BaseLayout.astro';
import Greeting from '../components/Greeting';
const pageTitle = "Home Page";
---
<BaseLayout pageTitle={pageTitle}>
	<h2>My awesome blog subtitle</h2>
	<!--<Greeting messages={["Hi", "Hello", "Howdy", "Hey there"]} />-->
	<Greeting client:load messages={["Hej", "Hallo", "Hola", "Habari", "Yo", "My boy", "'Sup"]} />
	<h4>if the new greeting button seems like not working, it's actually random and getting same result twice, keep on clicking</h4>
</BaseLayout>